{
  "name": "vertil-os",
  "version": "17.0.0",
  "scripts": {
    "build": "npx esbuild index.tsx --bundle --outfile=bundle.js --format=esm --jsx=automatic --minify --loader:.ts=ts --loader:.tsx=tsx"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}