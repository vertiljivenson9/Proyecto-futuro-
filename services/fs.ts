import { Inode } from '../types';
export const initFS = async () => {};
export const getInode = async (path: string) => null;
export const saveFile = async (path: string, content: string, ns: string) => {};
export const listDir = async (path: string) => [];