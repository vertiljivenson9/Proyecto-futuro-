import React, { useState, useEffect, useCallback, useRef } from 'react';import Desktop from './components/Desktop';import { initFS } from './services/fs';import { initRegistry } from './services/registry';export default function App() { useEffect(() => { initFS().then(initRegistry); }, []); return <Desktop />; }